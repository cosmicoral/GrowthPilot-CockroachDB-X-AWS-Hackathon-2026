# Leave empty when using the local Vite proxy.
VITE_API_BASE_URL=

# For a separately deployed API:
# VITE_API_BASE_URL=https://api.example.com

# Optional local proxy override; defaults to the FastAPI dev server.
VITE_BACKEND_PROXY_TARGET=http://localhost:8000
